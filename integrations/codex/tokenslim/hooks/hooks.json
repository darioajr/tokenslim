{
  "hooks": {
    "PostToolUse": [{
      "matcher": "^Bash$",
      "hooks": [{
        "type": "command",
        "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/tokenslim\" hook post-tool-use --agent codex",
        "timeout": 10
      }]
    }]
  }
}
